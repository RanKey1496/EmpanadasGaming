<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/background"
    tools:context="co.edu.udem.lenguajes2.empanadasgaming.MainActivity" >

    <Button
        android:id="@+id/btnSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/txtSummonerID"
        android:layout_alignRight="@+id/spRegions"
        android:layout_below="@+id/spRegions"
        android:text="@string/btnSearch" />

    <Spinner
        android:id="@+id/spRegions"
        android:layout_width="70dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/txtSummonerID" />

    <EditText
        android:id="@+id/txtSummonerID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_marginBottom="160dp"
        android:layout_toLeftOf="@+id/spRegions"
        android:ems="10"
        android:hint="@string/txtSummonerHint" >

        <requestFocus />
    </EditText>
    
</RelativeLayout>
